version: '3.8'

services:
  mongodb:
    image: mongo:latest
    container_name: diabetessupport-mongo
    restart: always
    ports:
      - '27017:27017'
    environment:
      MONGO_INITDB_DATABASE: diabetessupport
    volumes:
      - mongodb_data:/data/db

volumes:
  mongodb_data:
    driver: local
